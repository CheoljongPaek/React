import { styled as muistyled, Paper, AppBar } from '@material-ui/core';
import { Box } from '@material-ui/system';

/* main page write */
export const WriteContainer = muistyled(AppBar)(({theme}) => ({
}));