import React, { memo, useState } from "react";
import { Route, Switch } from 'react-router';

const Order = () => {

  return (
    <>
      <div>order</div>

    </>
  );
};

export default memo(Order);